# Memoria — game-planner

Historial de juegos propuestos, considerados y descartados para Arcade Vault. Lo mantiene el agente `game-planner`; no editar a mano salvo para corregir un estado.

Estados: `implementado` · `propuesto` · `considerado` · `descartado`.

| Fecha      | Juego                       | Id destino      | Estado       | Veredicto                                                                                                                                                                                                                                                                                                                                                   |
| ---------- | --------------------------- | --------------- | ------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 2026-07-26 | Asteroids                   | `rocas`         | implementado | Port 1:1 de `references/started-games/02-asteroids` — spec 05.                                                                                                                                                                                                                                                                                              |
| 2026-07-27 | Tetris                      | `caida`         | implementado | Port 1:1 de `references/started-games/03-tetris` — spec 07.                                                                                                                                                                                                                                                                                                 |
| 2026-07-28 | Arkanoid                    | `bloque-buster` | implementado | Port 1:1 de `references/started-games/04-arkanoid` — spec 08.                                                                                                                                                                                                                                                                                               |
| 2026-07-29 | Snake                       | `serpentina`    | implementado | Diseño desde cero con `source-assets/snake-assets` — spec 09.                                                                                                                                                                                                                                                                                               |
| 2026-07-31 | Pong                        | `duelo-pixel`   | propuesto    | Único candidato que abre categoría VERSUS (0/8 real hoy); esfuerzo bajo, reusa placeholder sin migración.                                                                                                                                                                                                                                                   |
| 2026-07-31 | Space Invaders              | `invasores`     | propuesto    | Segunda ronda del 2026-07-31 (Pong excluido a pedido): mejor candidato restante, equilibra SHOOTER (1→2 reales) sin sumar a ARCADE (ya 2 reales + 2 placeholders).                                                                                                                                                                                          |
| 2026-07-31 | Frogger                     | `ranaria`       | propuesto    | Tercera ronda del 2026-07-31 (Pong e Invasores excluidos a pedido): con solo `gloton` y `ranaria` libres, gana por menor esfuerzo — vidas y multi-carril sin IA de persecución, frente al pathfinding de 4 fantasmas de Pac-Man.                                                                                                                            |
| 2026-07-31 | Pac-Man                     | `gloton`        | propuesto    | Cuarta ronda del 2026-07-31 (Pong, Invasores y Frogger excluidos a pedido): único placeholder sin proponer; gana por prioridad de fuente (placeholder > id nuevo) pese a ser el más caro de los cuatro (laberinto + IA de 4 fantasmas).                                                                                                                     |
| 2026-07-31 | 2048                        | `fusion`        | propuesto    | Quinta ronda: con los cuatro placeholders agotados, gana como mejor candidato de id nuevo — tapa el hueco real de PUZZLE (1/8 con motor) con el menor esfuerzo del lote y sin dependencias de física ni IA.                                                                                                                                                 |
| 2026-07-31 | Centipede                   | _(nuevo)_       | considerado  | Alternativa de id nuevo evaluada en la quinta ronda; queda segunda por mayor esfuerzo (segmentos encadenados + IA de araña/pulga/escorpión) y por no abrir categoría nueva (profundizaría SHOOTER/ARCADE, ya cubiertos).                                                                                                                                    |
| 2026-07-31 | Tron (motos de luz)         | `estela`        | propuesto    | Sexta ronda: gana por ser el mejor refuerzo de VERSUS (1/8 proyectado, el hueco más grande si se cuentan también los cuatro `propuesto` pendientes), con mecánica distinta a Pong (estela persistente, no rebote) y esfuerzo comparable a `serpentina`.                                                                                                     |
| 2026-07-31 | Missile Command             | _(nuevo)_       | considerado  | Alternativa de la sexta ronda: mouse-only y sin física pesada, pero refuerza SHOOTER (ya en 2/8 proyectado con `invasores`), menos urgente que VERSUS; queda segunda.                                                                                                                                                                                       |
| 2026-07-31 | Centipede                   | _(nuevo)_       | considerado  | Reevaluado en la sexta ronda junto a ideas frescas: sigue perdiendo por esfuerzo alto y por no abrir ni reforzar la categoría más débil (VERSUS).                                                                                                                                                                                                           |
| 2026-07-31 | Missile Command             | `impacto`       | propuesto    | Séptima ronda: distinto de los seis ya `propuesto`; con el catálogo proyectado ya balanceado (ARCADE 4, PUZZLE 2, SHOOTER 2, VERSUS 2) gana por menor riesgo/esfuerzo frente a un match-3 (cascadas) y por vidas/nivel de mapeo directo (ciudades restantes / oleada).                                                                                      |
| 2026-07-31 | Match-3 (tipo Bejeweled)    | _(nuevo)_       | considerado  | Idea fresca de la séptima ronda: reforzaría PUZZLE (2→3) con mecánica nueva (intercambio + cascada), pero el algoritmo de detección de coincidencias + relleno en cascada es más propenso a bugs que la física simple de Missile Command; queda segunda.                                                                                                    |
| 2026-07-31 | Q\*bert                     | _(nuevo)_       | considerado  | Idea fresca de la séptima ronda: movimiento isométrico en diagonal es atípico para el resto del catálogo (todo ortogonal) y profundiza ARCADE, ya la categoría más cargada (4 proyectados); descartado por esfuerzo/riesgo y por no aportar variedad.                                                                                                       |
| 2026-07-31 | Centipede                   | _(nuevo)_       | considerado  | Reevaluado por tercera vez (séptima ronda) junto a Match-3 y Q\*bert: sigue siendo el más caro (segmentos encadenados + 3 IAs) y sin ventaja de categoría sobre Missile Command.                                                                                                                                                                            |
| 2026-07-31 | Simón (secuencia)           | `secuencia`     | propuesto    | Octava ronda: idea fresca de PUZZLE (memoria de secuencia, sin física ni cascada); gana por ser el menor esfuerzo/riesgo del lote y reforzar PUZZLE (2/8 proyectado, empatado con VERSUS como categoría más chica) sin duplicar mecánica de `caida` ni `fusion`.                                                                                            |
| 2026-07-31 | Match-3 (tipo Bejeweled)    | _(nuevo)_       | considerado  | Reevaluado en la octava ronda: mismo riesgo de siempre (detección de coincidencias + cascada + re-chequeo de combos), más propenso a bugs que la máquina de estados simple de Simón; queda segunda.                                                                                                                                                         |
| 2026-07-31 | Q\*bert                     | _(nuevo)_       | considerado  | Reevaluado en la octava ronda: sigue siendo movimiento isométrico diagonal, atípico frente al resto del catálogo (todo ortogonal o vectorial) y profundiza ARCADE, ya la categoría más cargada (4 proyectado); se mantiene descartado de entrada.                                                                                                           |
| 2026-07-31 | Centipede                   | _(nuevo)_       | considerado  | Reevaluado por cuarta vez (octava ronda): sigue siendo el más caro (segmentos encadenados + 3 IAs distintas) y no aporta ventaja de categoría sobre Simón.                                                                                                                                                                                                  |
| 2026-07-31 | Boxeo (Nocaut)              | `nocaut`        | propuesto    | Novena ronda: quinto id nuevo, distinto de los ocho ya `propuesto`; gana como el tercer refuerzo de VERSUS (empataría la categoría más chica del proyectado con PUZZLE y SHOOTER en 3) con el menor esfuerzo del lote — sin maze, sin física de balas, solo distancia + temporizador de golpe/bloqueo.                                                      |
| 2026-07-31 | Combate de tanques          | _(nuevo)_       | considerado  | Alternativa fresca de la novena ronda: también refuerza VERSUS con mecánica de proyectiles distinta a Pong/Tron, pero exige maze de obstáculos + rebote de balas en pared + IA de puntería, más caro que Nocaut; queda segunda.                                                                                                                             |
| 2026-07-31 | Memorice (parejas)          | `parejas`       | propuesto    | Décima ronda: sexto id nuevo, distinto de los nueve ya propuestos; con las tres categorías no-ARCADE empatadas en 3 tras nueve rondas, la variedad deja de discriminar y gana por ser el candidato de menor esfuerzo/riesgo del lote (máquina de estados voltear-comparar-ocultar, sin física ni IA).                                                       |
| 2026-07-31 | Combate de tanques          | _(nuevo)_       | considerado  | Reevaluado en la décima ronda junto a Memorice: sigue exigiendo maze + rebote de balas + IA de puntería, más caro que un simple flip-and-match; queda segunda.                                                                                                                                                                                              |
| 2026-07-31 | Match-3 (tipo Bejeweled)    | _(nuevo)_       | considerado  | Reevaluado en la décima ronda: mismo riesgo de siempre (detección de coincidencias + cascada + re-chequeo de combos), más propenso a bugs que Memorice; queda tercera.                                                                                                                                                                                      |
| 2026-07-31 | Galería (caza de patos)     | `galeria`       | propuesto    | Undécima ronda: séptimo id nuevo, distinto de los diez ya propuestos; galería de tiro mouse-only, refuerza SHOOTER (empataría con ARCADE/PUZZLE en 4 proyectado) con menor esfuerzo que cualquier VERSUS pendiente — sin IA de formación, sin física de rebote, solo spawn/movimiento de blancos + hit-test de clic.                                        |
| 2026-07-31 | Combate de tanques          | _(nuevo)_       | considerado  | Reevaluado por tercera vez (undécima ronda): sigue exigiendo maze + rebote de balas + IA de puntería, más caro que Galería; queda segunda.                                                                                                                                                                                                                  |
| 2026-07-31 | Whack-a-mole (topos)        | _(nuevo)_       | considerado  | Idea fresca de la undécima ronda: esfuerzo comparable a Galería (spawn timers + hit-test de clic), pero profundiza ARCADE a 5 proyectado en vez de equilibrar SHOOTER; queda segunda por peor efecto de categoría.                                                                                                                                          |
| 2026-07-31 | Conecta 4                   | `conecta4`      | propuesto    | Duodécima ronda: octavo id nuevo, distinto de los once ya propuestos; refuerza VERSUS (única categoría rezagada, 3 proyectado) con esfuerzo menor a Combate de tanques — sin maze, sin física de rebote, solo apilado por columna + chequeo de 4 en línea + IA mínima gana/bloquea.                                                                         |
| 2026-07-31 | Reversi (Otelo)             | _(nuevo)_       | considerado  | Alternativa fresca de la duodécima ronda: también refuerza VERSUS con IA mínima y sin física, pero su lógica de volteo en 8 direcciones es más propensa a bugs que el chequeo de 4-en-línea de Conecta 4; queda segunda.                                                                                                                                    |
| 2026-07-31 | Combate de tanques          | _(nuevo)_       | considerado  | Reevaluado por cuarta vez (duodécima ronda): sigue exigiendo maze + rebote de balas + IA de puntería, más caro que Conecta 4 pese a tapar el mismo hueco de categoría (VERSUS); queda tercera.                                                                                                                                                              |
| 2026-07-31 | Whack-a-mole (topos)        | `topos`         | propuesto    | Decimotercera ronda: con las cuatro categorías empatadas en 4 proyectado, la variedad deja de discriminar y el criterio pasa a ser 100% esfuerzo/riesgo; gana como el candidato de menor esfuerzo absoluto — grilla fija de agujeros + timers de aparición + hit-test de clic, sin trayectorias ni IA, más simple incluso que Galería.                      |
| 2026-07-31 | Reversi (Otelo)             | _(nuevo)_       | considerado  | Reevaluado segunda vez (decimotercera ronda): volteo en 8 direcciones sigue siendo más propenso a bugs de "movimiento inválido" que un simple hit-test de clic; queda segunda.                                                                                                                                                                              |
| 2026-07-31 | Combate de tanques          | _(nuevo)_       | considerado  | Reevaluado por quinta vez (decimotercera ronda): sigue siendo el más caro de todos los candidatos de VERSUS (maze + rebote de balas + IA de puntería); queda tercera.                                                                                                                                                                                       |
| 2026-07-31 | Buscaminas                  | `minas`         | propuesto    | Decimocuarta ronda: décimo id nuevo, distinto de los trece ya propuestos; gana en criterio 100% esfuerzo/riesgo porque es el único candidato del pool acumulado que no requiere ninguna IA (a diferencia de Reversi/Conecta4) ni algoritmo de cascada propenso a bugs (a diferencia de Match-3) — solo flood-fill iterativo + conteo de minas adyacentes.   |
| 2026-07-31 | Reversi (Otelo)             | _(nuevo)_       | considerado  | Reevaluado por tercera vez (decimocuarta ronda): sigue exigiendo IA (aunque heurística simple) + volteo en 8 direcciones con casos límite de "sin flancos que voltear", más caro/riesgoso que Buscaminas, que no necesita oponente.                                                                                                                         |
| 2026-07-31 | Combate de tanques          | _(nuevo)_       | considerado  | Reevaluado por sexta vez (decimocuarta ronda): sigue siendo el más caro de todos los candidatos de VERSUS (maze + rebote de balas + IA de puntería); queda por debajo de Buscaminas en esfuerzo/riesgo.                                                                                                                                                     |
| 2026-07-31 | Flappy Bird (estilo)        | `vuelo`         | propuesto    | Decimoquinta ronda: undécimo id nuevo, distinto de los catorce ya propuestos; gana el criterio 100% esfuerzo/riesgo por ser el único candidato del pool acumulado sin IA, sin cascada y sin turnos — solo gravedad + salto + colisión AABB contra tuberías, más simple que `serpentina`.                                                                    |
| 2026-07-31 | Solitario (Klondike)        | _(nuevo)_       | considerado  | Idea fresca de la decimoquinta ronda: las reglas de movimiento entre pilas (fundación ascendente por palo, tablero descendente alternando color, solo reyes en columna vacía, traslado de secuencias) son más numerosas y propensas a bugs de validación que la física de `vuelo`; queda segunda.                                                           |
| 2026-07-31 | Buscapalabras               | _(nuevo)_       | considerado  | Idea fresca de la decimoquinta ronda: requiere un algoritmo de colocación de palabras en 8 direcciones con backtracking para evitar conflictos de letras superpuestas, más riesgo de bugs sutiles de generación que la física simple de `vuelo`; queda tercera.                                                                                             |
| 2026-07-31 | Tres en raya (Gato)         | `gato`          | propuesto    | Decimosexta ronda: duodécimo id nuevo, distinto de los quince ya propuestos; gana el criterio 100% esfuerzo/riesgo por ser el candidato de menor código y menor superficie de bugs de todo el pool acumulado — tablero 3×3, chequeo de 8 líneas, IA heurística de 3 reglas (ganar/bloquear/centro-esquina) sin backtracking ni cascada ni gestión de pilas. |
| 2026-07-31 | Solitario (Klondike)        | _(nuevo)_       | considerado  | Reevaluado en la decimosexta ronda como cabeza de la cola de respaldos: sin IA, pero la gestión de 7 columnas + 4 fundaciones + robo/reciclado de mazo + traslado de secuencias completas exige más estado y casos límite de validación que un tablero 3×3; queda segunda.                                                                                  |
| 2026-07-31 | Reversi (Otelo)             | _(nuevo)_       | considerado  | Reevaluado por cuarta vez (decimosexta ronda): volteo en 8 direcciones sobre un tablero 8×8 más una IA de flancos sigue siendo más código y más casos límite ("sin flancos que voltear", pase de turno obligado) que el tablero 3×3 de Gato; queda tercera.                                                                                                 |
| 2026-07-31 | Ahorcado                    | `verdugo`       | propuesto    | Decimoséptima ronda: decimotercer id nuevo, distinto de los dieciséis ya propuestos; gana el criterio 100% esfuerzo/riesgo por ser el candidato de menor superficie de código de todo el pool acumulado — sin IA, sin generación con backtracking, sin grid ni win-line checking, solo lista de palabras + set de letras adivinadas + contador de fallos.   |
| 2026-07-31 | Solitario (Klondike)        | _(nuevo)_       | considerado  | Reevaluado en la decimoséptima ronda como cabeza de la cola de respaldos: sin IA, pero la gestión de 7 columnas + 4 fundaciones + robo/reciclado de mazo + traslado de secuencias completas exige bastante más estado y validación que el modelo trivial de Ahorcado; queda segunda.                                                                        |
| 2026-07-31 | Lights Out                  | _(nuevo)_       | considerado  | Idea fresca de la decimoséptima ronda: grid con toggle en cruz, sin IA y con generación trivialmente solvable, pero exige lógica de propagación de toggle sobre una grilla que Ahorcado no necesita; queda tercera.                                                                                                                                         |
| 2026-07-31 | Mastermind (código secreto) | `codigo`        | propuesto    | Decimoctava ronda: decimocuarto id nuevo, distinto de los diecisiete ya propuestos; gana el criterio 100% esfuerzo/riesgo por ser el único candidato del pool acumulado sin IA de ningún tipo (ni heurística ni fórmula), sin grilla, sin cascada y sin turnos alternados — solo secreto aleatorio + función de comparación exacta/color.                   |
| 2026-07-31 | Nim (juego de fichas)       | _(nuevo)_       | considerado  | Idea fresca de la decimoctava ronda: sin heurística (AI por fórmula de nim-sum, XOR de pilas) y sin grilla, pero exige turnos alternados + chequeo de estado terminal por pila, más superficie que la comparación directa de Mastermind; queda segunda.                                                                                                     |
| 2026-07-31 | Solitario (Klondike)        | _(nuevo)_       | considerado  | Reevaluado por cuarta vez (decimoctava ronda): sigue exigiendo gestión de 7 columnas + 4 fundaciones + robo/reciclado de mazo + traslado de secuencias completas, muy por encima en estado y validación de cualquier candidato de esta ronda; queda tercera.                                                                                                |
| 2026-07-31 | Nim (juego de fichas)       | `montones`      | propuesto    | Decimonovena ronda: cabeza de la cola de respaldos desde la decimoctava ronda; gana por ser el único candidato restante con IA por fórmula cerrada y determinista (nim-sum) en vez de heurística ad-hoc, esfuerzo/riesgo por debajo de todo el `considerado` acumulado.                                                                                     |
| 2026-07-31 | Damas (Checkers)            | _(nuevo)_       | considerado  | Idea fresca de la decimonovena ronda: tablero 8×8 con captura obligatoria, coronación a dama y saltos múltiples encadenados, bastante más caro que la fórmula de nim-sum de Nim; queda segunda.                                                                                                                                                             |
| 2026-07-31 | Solitario (Klondike)        | _(nuevo)_       | considerado  | Reevaluado por quinta vez (decimonovena ronda): sigue exigiendo gestión de 7 columnas + 4 fundaciones + robo/reciclado de mazo, muy por encima en estado que Nim; queda tercera.                                                                                                                                                                            |
| 2026-07-31 | Lights Out                  | `apagon`        | propuesto    | Vigésima ronda (final, 20/20): único candidato del pool `considerado` sin ningún oponente CPU (a diferencia de Reversi/Damas/Combate de tanques) ni algoritmo de generación/cascada riesgoso (a diferencia de Solitario/Buscapalabras/Match-3) — solo una función de toggle sobre grilla N×N, generación trivialmente solvable desde el estado resuelto.    |
| 2026-07-31 | Solitario (Klondike)        | _(nuevo)_       | considerado  | Reevaluado por sexta vez (vigésima ronda, final): sin IA, pero su gestión de 7 columnas + 4 fundaciones + robo/reciclado de mazo + traslado de secuencias completas exige mucho más estado que una simple función de toggle; queda segunda.                                                                                                                 |
| 2026-07-31 | Reversi (Otelo)             | _(nuevo)_       | considerado  | Reevaluado por quinta vez (vigésima ronda, final): sigue exigiendo IA + volteo en 8 direcciones con casos límite de "sin flancos que voltear", más caro que Lights Out; queda tercera.                                                                                                                                                                      |

## Notas

### Estado de las fuentes (2026-07-30)

- `references/started-games/` — las tres carpetas (`02-asteroids`, `03-tetris`, `04-arkanoid`) ya están portadas. No queda código de referencia sin usar.
- `references/source-assets/` — solo `snake-assets`, ya consumido por `serpentina`.
- Consecuencia: todo candidato nuevo es **diseño desde cero**, salvo que se agreguen fuentes nuevas a `references/`.

### Placeholders libres

`gloton` (Pac-Man), `invasores` (Space Invaders), `ranaria` (Frogger), `duelo-pixel` (Pong). Ninguno tiene motor; usarlos evita migración de Supabase.

### Evaluación 2026-07-31

Con `references/started-games/` agotado y `source-assets/` sin material nuevo, los cuatro candidatos son los placeholders, todos "desde cero". Se priorizó variedad de catálogo: VERSUS está en 0/8 juegos reales (`duelo-pixel` sin portar) mientras ARCADE ya tiene 2 reales (`bloque-buster`, `serpentina`) y SHOOTER 1 (`rocas`). Pong gana por ser el hueco de categoría más grande y el de menor esfuerzo de los cuatro. `invasores`, `ranaria` y `gloton` quedan como `considerado`, no `descartado` — siguen disponibles para una próxima ronda si `duelo-pixel` se implementa o si cambian las condiciones (p. ej. aparece código de referencia para alguno en `references/`).

### Segunda ronda 2026-07-31 (Pong excluido a pedido)

Mismo día, se pide excluir `duelo-pixel`/Pong explícitamente (sigue `propuesto`, pendiente de aceptación, no se toca su fila). Fuentes sin cambios: `references/started-games/` sigue agotado, `references/source-assets/` sin material nuevo, sin specs nuevas en `specs/`. Entre los tres placeholders restantes (`invasores`, `ranaria`, `gloton`), `invasores` sube de `considerado` a `propuesto`: es el que mejor equilibra el catálogo (SHOOTER pasa de 1 a 2 reales, igualando a ARCADE, en vez de profundizar el desbalance llevando ARCADE a 3 reales) y su esfuerzo (formación fija + IA de disparo simple) es menor que Frogger (multi-carril con temporizador) y muy inferior a Pac-Man (laberinto + pathfinding de 4 fantasmas). `ranaria` y `gloton` se mantienen `considerado`.

### Tercera ronda 2026-07-31 (Pong e Invasores excluidos a pedido)

Mismo día, se pide excluir también `invasores`/Space Invaders (sigue `propuesto`, pendiente de aceptación, fila sin tocar). Fuentes sin cambios respecto a las rondas anteriores: `references/started-games/` agotado, `references/source-assets/` sin material nuevo, sin specs nuevas en `specs/`. Solo quedan dos placeholders libres, `ranaria` y `gloton`, ambos ARCADE — la variedad de catálogo deja de ser un criterio distintivo porque cualquiera de los dos deja ARCADE en 3 reales. El desempate cae en esfuerzo y riesgo: Frogger (`ranaria`) tiene concepto de vidas nativo (choque con auto o caída al río) y nivel natural (ronda completada → sube velocidad de carriles), con lógica de colisión multi-carril pero sin IA. Pac-Man (`gloton`) exige laberinto navegable + IA de persecución para 4 fantasmas con modo asustado, bastante más caro y más riesgoso de balancear sin código de referencia. `ranaria` sube de `considerado` a `propuesto`; `gloton` se mantiene `considerado` como respaldo si `ranaria` se descarta o se acepta primero.

### Cuarta ronda 2026-07-31 (Pong, Invasores y Frogger excluidos a pedido)

Mismo día, se pide excluir también `ranaria`/Frogger (sigue `propuesto`, pendiente de aceptación, fila sin tocar). Se reconfirmó con `Glob` sobre `references/**/*` que `references/started-games/` solo contiene `02-asteroids`, `03-tetris` y `04-arkanoid` (las tres ya portadas, más un `.DS_Store` sin contenido útil) y que `references/source-assets/` solo tiene `snake-assets` (ya consumido por `serpentina`) — **agotado confirmado, no contradicho**. Con eso, `gloton` (Pac-Man) es el único placeholder que queda sin proponer, y por la prioridad de fuente de la Fase 2 (placeholder libre > id nuevo desde cero) le gana a cualquier candidato de id nuevo evaluado esta ronda: se consideró `2048` para tapar el hueco de PUZZLE (1/8 juegos reales hoy, el más desbalanceado si se cuentan solo motores reales), pero exige migración (fila en `games` + entrada en `GAMES` + nueva clase `cover-*`) que `gloton` no necesita. `gloton` sube de `considerado` a `propuesto`. **Con esta ronda se agotan los cuatro placeholders** (`duelo-pixel`, `invasores`, `ranaria`, `gloton` quedan todos en estado `propuesto`); la próxima ronda, si ninguno de los cuatro se acepta o implementa antes, tendrá que evaluar candidatos de id nuevo desde cero (p. ej. `2048` u otro para cubrir PUZZLE/VERSUS reales).

### Quinta ronda 2026-07-31 (placeholders agotados, toca id nuevo)

Confirmado que los cuatro placeholders (`duelo-pixel`, `invasores`, `ranaria`, `gloton`) siguen en `propuesto` sin aceptación ni descarte — no se tocan sus filas. Se reconfirmó de nuevo con `Glob` que `references/started-games/` y `references/source-assets/` no tienen material nuevo (mismo estado que la ronda anterior): todo candidato de esta ronda es id nuevo + diseño desde cero, con el costo fijo de migración (fila en `games`, entrada en `GAMES`, clase `cover-*` nueva). Contando solo motores reales, la categoría más desbalanceada es PUZZLE (1/8, únicamente `caida`) frente a ARCADE (2/8) y SHOOTER (1/8, más `invasores` ya en camino). Se evaluó `2048` contra `Centipede`: 2048 encaja de forma trivial en el contrato (grid 4×4, sin física, sin IA, sin colisión de sprites, controles solo de flechas) y con un esfuerzo estimado por debajo de `serpentina` (258 líneas), mientras que Centipede exige segmentos encadenados que se dividen al recibir disparos más IA de tres enemigos distintos (araña errática, pulga que planta hongos, escorpión que envenena), esfuerzo alto y sin abrir categoría nueva (profundizaría SHOOTER/ARCADE, ya cubiertos por lo pendiente). `2048` gana y se propone con id nuevo `fusion`; `Centipede` queda `considerado` como respaldo si `fusion`/2048 se descarta.

**Candidatos ya agotados para próximas rondas:** los cuatro placeholders (`propuesto`, pendientes de aceptación) y `2048`→`fusion` (`propuesto` en esta ronda). Si se pide excluir `fusion` en una sexta ronda, el respaldo inmediato es `Centipede`, y más allá de eso habrá que buscar un tercer candidato de id nuevo desde cero (p. ej. Missile Command, Q*bert o un match-3 tipo Bejeweled) evaluando de nuevo variedad de catálogo y esfuerzo.

### Sexta ronda 2026-07-31 (segundo id nuevo, distinto de los cinco ya propuestos y de Centipede)

Se pide un id nuevo distinto de los cinco ya `propuesto` (Pong/`duelo-pixel`, Invasores/`invasores`, Frogger/`ranaria`, Pac-Man/`gloton`, 2048/`fusion`) y distinto de `Centipede` salvo que siga siendo el mejor candidato. Se reconfirmó (`Glob` sobre `references/**/*`) que no hay fuentes nuevas: `references/started-games/` sigue con solo las tres carpetas ya portadas y `references/source-assets/` solo con `snake-assets` ya consumido — todo candidato de esta ronda es diseño desde cero con costo fijo de migración. El análisis de variedad cambió de eje: contando el catálogo **proyectado** (los cuatro placeholders + `fusion`, todos `propuesto` pero ninguno aceptado/implementado todavía), las categorías quedarían ARCADE 4 (`bloque-buster`, `serpentina`, `gloton`, `ranaria`), PUZZLE 2 (`caida`, `fusion`), SHOOTER 2 (`rocas`, `invasores`) y VERSUS 1 (solo `duelo-pixel`) — VERSUS sigue siendo el hueco más profundo incluso después de todo lo ya propuesto, mientras que reforzar PUZZLE o SHOOTER otra vez (p. ej. con Missile Command) profundizaría categorías que ya van a quedar en paridad con 2. Se evaluaron tres ideas frescas contra `Centipede`: `Tron` (motos de luz con estela persistente, colisión contra muro/estela propia/estela rival), `Missile Command` (defensa de ciudades a mouse) y `Simon` (secuencia de memoria). `Tron` gana: abre una segunda entrada real para VERSUS con una mecánica distinta a Pong (estela persistente vs. rebote de pelota), con un esfuerzo estimado comparable a `serpentina` (sin física de balón, sin colisión de sprites, solo grilla de estela + una IA simple de "evitar colisión inmediata" para el rival CPU). `Missile Command` queda `considerado`: viable y de bajo/medio esfuerzo, pero profundiza SHOOTER en vez de tapar el hueco de VERSUS. `Centipede`, reevaluado explícitamente contra estas ideas frescas, se mantiene `considerado`: sigue siendo el candidato más caro (segmentos encadenados + tres IAs distintas) y no mejora el balance de categoría. `Tron` se propone con id nuevo `estela`.

**Candidatos agotados tras esta ronda:** los cuatro placeholders, `fusion` (2048) y ahora `estela` (Tron), todos `propuesto` y pendientes de aceptación. Si se pide excluir `estela` en una séptima ronda, los respaldos son `Missile Command` o `Centipede` (ambos `considerado`), y más allá de esos dos habrá que buscar una cuarta idea de id nuevo (p. ej. Q*bert, un match-3 tipo Bejeweled, o un segundo VERSUS distinto como Air Hockey, aunque este último se descartaría de entrada por ser mecánicamente casi idéntico a Pong).

### Séptima ronda 2026-07-31 (tercer id nuevo, distinto de los seis ya propuestos)

Se pide un séptimo juego con id nuevo, distinto de los seis ya `propuesto` (Pong/`duelo-pixel`, Invasores/`invasores`, Frogger/`ranaria`, Pac-Man/`gloton`, 2048/`fusion`, Tron/`estela`) y de `Missile Command`/`Centipede` salvo que sigan siendo el mejor candidato tras comparar con ideas frescas. Se reconfirmó (`Glob` sobre `references/**/*` y `references/source-assets/**/*`) que las fuentes siguen agotadas: las tres carpetas de `references/started-games/` ya están portadas y `source-assets/` solo tiene `snake-assets`, ya consumido — séptimo candidato también es diseño desde cero con costo fijo de migración. El catálogo **proyectado** (8 ids actuales + `fusion` + `estela`, todos `propuesto` pero ninguno aceptado/implementado) queda ARCADE 4 (`bloque-buster`, `serpentina`, `gloton`, `ranaria`), PUZZLE 2 (`caida`, `fusion`), SHOOTER 2 (`rocas`, `invasores`) y VERSUS 2 (`duelo-pixel`, `estela`): por primera vez ninguna categoría está claramente vacía, así que el criterio de variedad ya no basta solo y pasa a pesar más el esfuerzo/riesgo relativo.

Se evaluaron dos ideas frescas contra los dos ya `considerado`: un **match-3 tipo Bejeweled** (grilla de gemas, intercambio adyacente, detección de coincidencias de 3+, cascada de relleno) y **Q\*bert** (cubos isométricos que cambian de color al saltar, enemigos que empujan al vacío). El match-3 reforzaría PUZZLE (2→3) con una mecánica de grilla nueva (ni caída tipo Tetris ni fusión tipo 2048), pero su algoritmo de match-detection + cascada + re-chequeo de combos es más propenso a bugs sutiles (deshacer swaps inválidos, detectar "sin movimientos posibles") que cualquier candidato ya evaluado. Q\*bert queda descartado de entrada: mueve al jugador en diagonales isométricas, un paradigma de input y render distinto a los ocho juegos existentes (todos ortogonales en grilla o vectoriales en el plano), y de paso profundiza ARCADE, ya la categoría más cargada del proyectado (4).

Contra estas dos, `Missile Command` (`considerado` desde la sexta ronda) sigue ganando: su modelo de entidades (misiles enemigos cayendo en línea recta desde arriba, clic para lanzar un interceptor que explota en un radio, y una IA nula — los enemigos no persiguen, solo caen) es más simple y con menos casos límite que el match-3, y su mapeo a vidas (ciudades restantes, arrancando en un número fijo y bajando una por cada ciudad destruida) y nivel (oleada, sube frecuencia/velocidad de misiles) es directo y ya precedentado por `rocas` (oleadas) y por el propio HUD de vidas discretas. `Centipede`, reevaluado por tercera vez, se mantiene `considerado`: sigue siendo el más caro (segmentos encadenados que se dividen + IA de araña/pulga/escorpión) sin aportar ventaja de categoría sobre `Missile Command`. `Missile Command` sube de `considerado` a `propuesto` con id nuevo `impacto`.

**Candidatos agotados tras esta ronda:** los cuatro placeholders, `fusion` (2048), `estela` (Tron) e `impacto` (Missile Command), todos `propuesto` y pendientes de aceptación. Si se pide excluir `impacto` en una octava ronda, el respaldo inmediato es `Centipede` (`considerado`, tercera vez evaluado) o el match-3 recién evaluado (`considerado`, primera vez); más allá de esos dos habrá que buscar una quinta idea de id nuevo genuinamente fresca — el espacio de clásicos arcade obvios y de bajo riesgo empieza a agotarse.

### Octava ronda 2026-07-31 (cuarto id nuevo, distinto de los siete ya propuestos)

Se pide un octavo juego con id nuevo, distinto de los siete ya `propuesto` (Pong/`duelo-pixel`, Invasores/`invasores`, Frogger/`ranaria`, Pac-Man/`gloton`, 2048/`fusion`, Tron/`estela`, Missile Command/`impacto`), con Match-3 y Q\*bert habilitados como opción explícita además de ideas frescas. Se reconfirmó (`Glob` sobre `specs/**` y `references/**/*`) que no hay specs nuevas (siguen 01–09) ni fuentes nuevas: `references/started-games/` con las tres carpetas ya portadas, `source-assets/` solo con `snake-assets` ya consumido — octavo candidato también es diseño desde cero con costo fijo de migración.

Catálogo **proyectado** (8 ids actuales + `fusion`, `estela`, `impacto`, todos `propuesto` pero ninguno aceptado/implementado): ARCADE 4 (`bloque-buster`, `serpentina`, `gloton`, `ranaria`), PUZZLE 2 (`caida`, `fusion`), SHOOTER 3 (`rocas`, `invasores`, `impacto`), VERSUS 2 (`duelo-pixel`, `estela`). PUZZLE y VERSUS quedan empatadas como categorías más chicas. No hay un tercer candidato de VERSUS viable y distinto (Air Hockey es mecánicamente casi idéntico a Pong, ya descartado de entrada en notas previas), así que el objetivo de variedad recae en PUZZLE.

Se evaluó una idea fresca — **Simón / secuencia de memoria** (4 cuadrantes de color que se iluminan en una secuencia creciente; el jugador la repite con clic o flechas; falla resta una vida, acierto pasa de ronda) — contra los dos respaldos ya `considerado` (Match-3, Q\*bert) y contra Centipede (reevaluado por cuarta vez). Simón gana con margen claro: es una máquina de estados simple (mostrar secuencia → bloquear input → leer input → comparar), sin física, sin colisión de sprites, sin IA y sin el riesgo de bugs de cascada/recombo que arrastra Match-3 desde la séptima ronda; encaja el input en el precedente de mouse-only de `bloque-buster` (clic en cuadrante) con fallback de flechas. Su mapeo a HUD es directo: vidas = intentos fallidos permitidos (empieza en 3, precedente de `bloque-buster`/`rocas`), nivel = longitud de la secuencia/ronda actual (precedente de `caida` derivando nivel de líneas), score = pasos correctos acumulados. Match-3 y Q\*bert quedan `considerado` sin cambios en su veredicto (cascada propensa a bugs; movimiento isométrico atípico y refuerzo de ARCADE, ya la categoría más cargada). Centipede, reevaluado por cuarta vez, sigue siendo el más caro y sin ventaja de categoría. Simón se propone con id nuevo `secuencia`.

**Candidatos agotados tras esta ronda:** los cuatro placeholders, `fusion` (2048), `estela` (Tron), `impacto` (Missile Command) y ahora `secuencia` (Simón), todos `propuesto` y pendientes de aceptación. Si se pide excluir `secuencia` en una novena ronda, los respaldos son `Match-3` o `Centipede` (ambos `considerado`, reevaluados varias veces) — Q\*bert sigue descartado de entrada por su paradigma isométrico. Más allá de esos, el espacio de clásicos arcade de bajo riesgo está prácticamente agotado; convendría revisar si alguno de los ocho candidatos ya `propuesto` puede aceptarse/implementarse antes de seguir sumando más.

### Novena ronda 2026-07-31 (quinto id nuevo, distinto de los ocho ya propuestos)

Se pide un noveno juego con id nuevo, distinto de los ocho ya `propuesto` (cuatro placeholders + `fusion`, `estela`, `impacto`, `secuencia`). Se reconfirmó (`Glob` sobre `specs/**` y `references/**/*`) que no hay specs nuevas (siguen 01–09) ni fuentes nuevas: `references/started-games/` con las tres carpetas ya portadas, `source-assets/` solo con `snake-assets` ya consumido — noveno candidato también es diseño desde cero con costo fijo de migración.

Catálogo **proyectado** (8 ids actuales + `fusion`, `estela`, `impacto`, `secuencia`, todos `propuesto` pero ninguno aceptado/implementado): ARCADE 4 (`bloque-buster`, `serpentina`, `gloton`, `ranaria`), PUZZLE 3 (`caida`, `fusion`, `secuencia`), SHOOTER 3 (`rocas`, `invasores`, `impacto`), VERSUS 2 (`duelo-pixel`, `estela`). VERSUS sigue siendo la categoría más chica del proyectado, incluso después de sumar `estela`/Tron en la sexta ronda.

Se descartó de entrada un tercer Pong/Air Hockey (mecánicamente casi idéntico a `duelo-pixel`, ya señalado en notas previas) y se evaluaron dos ideas frescas de VERSUS: **Boxeo** (estilo Atari Boxing — dos boxeadores en un ring, movimiento de acercamiento/retirada en un eje, golpes altos/bajos con ventana de impacto y bloqueo, sin física de proyectiles ni de balón) y **Combate de tanques** (estilo Atari Combat — dos tanques en un laberinto de obstáculos, disparo con rebote de balas en las paredes). Ambas abren una tercera mecánica de VERSUS distinta a rebote de pelota (Pong) y estela persistente (Tron). Boxeo gana por menor esfuerzo: no necesita diseño de laberinto ni física de rebote de proyectiles, solo una máquina de estados de distancia + temporizador de golpe/bloqueo (más simple que la física de `rocas` y comparable o por debajo de `bloque-buster`), y su render puede resolverse con primitivas vectoriales simples (sin spritesheet), consistente con el estilo de `rocas`/`bloque-buster`. Combate de tanques queda `considerado`: viable y con la misma ventaja de categoría, pero el maze + rebote de balas + IA de puntería lo encarece por encima de Boxeo.

Mapeo a HUD: vidas = knockdowns permitidos antes de KO (empieza en 3, precedente de `bloque-buster`/`rocas`); nivel = round actual (1–3, la CPU se vuelve más agresiva/rápida cada round, precedente de `caida` derivando nivel de progreso); score = golpes limpios acumulados. Controles solo de teclado (flechas para acercarse/retirarse, una tecla de golpe alto y otra de golpe bajo, opcional tecla de bloqueo) — sin mouse, sin touch. Boxeo se propone con id nuevo `nocaut`.

Con esta propuesta el catálogo proyectado (9 candidatos de id nuevo/placeholder, ninguno aceptado aún, más los 8 originales) quedaría ARCADE 4, PUZZLE 3, SHOOTER 3, VERSUS 3 — el balance más parejo logrado hasta ahora entre las cuatro categorías.

**Candidatos agotados tras esta ronda:** los cuatro placeholders, `fusion` (2048), `estela` (Tron), `impacto` (Missile Command), `secuencia` (Simón) y ahora `nocaut` (Boxeo), todos `propuesto` y pendientes de aceptación. Si se pide excluir `nocaut` en una décima ronda, el respaldo inmediato es `Combate de tanques` (`considerado`); más allá de eso, y con `Match-3`/`Centipede` ya descartados varias veces y Q\*bert descartado de entrada, el espacio de clásicos arcade de bajo riesgo y mecánica distinta está prácticamente agotado — la recomendación explícita para una décima ronda es revisar primero si alguno de los nueve candidatos `propuesto` puede aceptarse o implementarse antes de seguir generando ideas nuevas desde cero.

### Décima ronda 2026-07-31 (sexto id nuevo, distinto de los nueve ya propuestos; el usuario confirma seguir hasta 20 propuestas totales)

El usuario confirma explícitamente que quiere seguir generando propuestas hasta llegar a 20 en total, aceptando que la calidad/originalidad de los candidatos baje a medida que se agota el pool de clásicos arcade de bajo riesgo. Se pide un décimo juego con id nuevo, distinto de los nueve ya `propuesto` (cuatro placeholders + `fusion`, `estela`, `impacto`, `secuencia`, `nocaut`), permitiendo repetir Centipede/Match-3/Q\*bert/Combate de tanques si siguen siendo el mejor candidato restante.

Se reconfirmó (`Glob` sobre `specs/**`, `references/started-games/**` y `references/source-assets/**`) que no hay specs nuevas (siguen 01–09) ni fuentes nuevas: las tres carpetas de `references/started-games/` siguen portadas y `source-assets/` solo tiene `snake-assets`, ya consumido — décimo candidato también es diseño desde cero con costo fijo de migración.

Catálogo **proyectado** (8 ids actuales + los nueve ya `propuesto`, ninguno aceptado/implementado todavía): ARCADE 4 (`bloque-buster`, `serpentina`, `gloton`, `ranaria`), PUZZLE 3 (`caida`, `fusion`, `secuencia`), SHOOTER 3 (`rocas`, `invasores`, `impacto`), VERSUS 3 (`duelo-pixel`, `estela`, `nocaut`). Por primera vez las tres categorías no-ARCADE están **empatadas en 3**: agregar a cualquiera de ellas produce el mismo resultado agregado (una categoría sube a 4, empatando con ARCADE). La variedad de catálogo deja de discriminar entre PUZZLE/SHOOTER/VERSUS en esta ronda; el desempate recae por completo en esfuerzo y riesgo.

Se reevaluaron los dos respaldos existentes (`Combate de tanques` para VERSUS, `Match-3` para PUZZLE; Q\*bert se descarta de entrada otra vez por su paradigma isométrico atípico y por profundizar ARCADE, ya la categoría más cargada) contra una idea fresca: **Memorice / juego de parejas** (grilla de cartas boca abajo con símbolos duplicados; el jugador voltea dos por clic, si coinciden quedan reveladas, si no se ocultan tras una breve pausa; el tablero crece de tamaño en cada ronda completada). Memorice gana con margen claro sobre los tres: es una máquina de estados mínima (mostrar → comparar → ocultar/mantener), sin física, sin colisión de sprites, sin IA de ningún tipo — más simple que la física de rebote de balas de Combate de tanques y sin el riesgo de bugs de cascada/recombo que arrastra Match-3 desde la séptima ronda. Su costo estimado queda por debajo de `serpentina` (258 líneas), comparable a `secuencia`.

Mapeo a HUD (el juego original no tiene ninguno de los tres conceptos, mismo caso de diseño que `caida`/`serpentina` fijando valores ausentes): vidas = intentos fallidos permitidos, empieza en un número fijo (p. ej. 5) y baja uno por cada par que no coincide, game over al llegar a 0; nivel = ronda completada (tablero resuelto por completo), sube y agranda la grilla siguiente (4×4 → 6×6 → 8×8), precedente de `caida` derivando nivel de progreso; score = puntos por pareja acertada, con bonus por racha sin fallos. Controles: solo mouse (clic en carta), sin teclado necesario — mismo paradigma mouse-only ya usado en `bloque-buster`.

`Combate de tanques` queda `considerado` (reevaluado): sigue exigiendo maze de obstáculos + rebote de balas en pared + IA de puntería, más caro que un simple flip-and-match. `Match-3` queda `considerado` (reevaluado): mismo riesgo de siempre (detección de coincidencias + cascada + re-chequeo de combos + "sin movimientos posibles"). Memorice se propone con id nuevo `parejas`.

Con esta propuesta el catálogo proyectado (10 candidatos de id nuevo/placeholder, ninguno aceptado aún, más los 8 originales) quedaría ARCADE 4, PUZZLE 4 (`caida`, `fusion`, `secuencia`, `parejas`), SHOOTER 3, VERSUS 3 — balance parejo, con PUZZLE alcanzando a ARCADE.

**Candidatos agotados tras esta ronda:** los cuatro placeholders, `fusion` (2048), `estela` (Tron), `impacto` (Missile Command), `secuencia` (Simón), `nocaut` (Boxeo) y ahora `parejas` (Memorice), todos `propuesto` y pendientes de aceptación — **diez propuestas en total**, faltan diez para la meta de 20 acordada con el usuario. Si se pide excluir `parejas` en una undécima ronda, los respaldos son `Combate de tanques` o `Match-3` (ambos `considerado`, reevaluados varias veces); Q\*bert y Centipede siguen descartados de entrada. Más allá de esos dos respaldos, el pool de clásicos arcade de bajo riesgo está agotado de verdad: las próximas rondas tendrán que recurrir a variantes menos canónicas (juegos de mesa adaptados tipo Conecta 4/Reversi con IA mínima, minijuegos de reacción tipo whack-a-mole, o segundas vueltas de mecánicas ya cubiertas con skins distintas), tal como el usuario ya anticipó y aceptó.

### Undécima ronda 2026-07-31 (séptimo id nuevo, distinto de los diez ya propuestos)

Se pide un undécimo juego con id nuevo, distinto de los diez ya `propuesto` (cuatro placeholders + `fusion`, `estela`, `impacto`, `secuencia`, `nocaut`, `parejas`), permitiendo retomar `Combate de tanques`/`Match-3`/`Centipede`/Q\*bert desde `considerado` si siguen siendo el mejor candidato, o traer una idea fresca. Se reconfirmó (`Glob` sobre `specs/**`, `references/started-games/**` y `references/source-assets/**`) que no hay specs nuevas (siguen 01–09) ni fuentes nuevas: las tres carpetas de `references/started-games/` siguen portadas y `source-assets/` solo tiene `snake-assets`, ya consumido — undécimo candidato también es diseño desde cero con costo fijo de migración.

Catálogo **proyectado** (8 ids actuales + los diez ya `propuesto`, ninguno aceptado/implementado todavía): ARCADE 4 (`bloque-buster`, `serpentina`, `gloton`, `ranaria`), PUZZLE 4 (`caida`, `fusion`, `secuencia`, `parejas`), SHOOTER 3 (`rocas`, `invasores`, `impacto`), VERSUS 3 (`duelo-pixel`, `estela`, `nocaut`). SHOOTER y VERSUS quedan empatadas como categorías más chicas del proyectado.

Se reevaluó `Combate de tanques` (VERSUS, `considerado` desde la novena ronda) contra dos ideas frescas: **Galería de tiro / caza de patos** (blancos que cruzan la pantalla en trayectorias simples — arcos o líneas rectas —, el jugador dispara con clic del mouse, blancos no acertados restan una vida, cada ronda sube la velocidad y la cantidad de blancos simultáneos) y **Whack-a-mole / topos** (grilla de agujeros, topos que asoman por tiempos aleatorios, clic para golpear, fallo o topo escapado resta vida, ronda sube frecuencia de aparición).

Galería gana: refuerza SHOOTER (empataría con ARCADE/PUZZLE en 4 proyectado, dejando VERSUS como única categoría en 3) con un esfuerzo por debajo de `invasores` — no hay formación ni IA de disparo enemigo, solo entidades independientes con una trayectoria de spawn/movimiento y un hit-test de clic contra su hitbox, comparable o inferior a `serpentina` (258 líneas). Whack-a-mole es de esfuerzo casi idéntico (spawn timers + hit-test de clic) pero profundizaría ARCADE a 5 proyectado, el peor resultado de balance posible en esta ronda; queda `considerado` como respaldo de bajo esfuerzo si Galería se descarta. `Combate de tanques`, reevaluado por tercera vez, se mantiene `considerado`: sigue exigiendo maze de obstáculos + rebote de balas en pared + IA de puntería, más caro que Galería pese a tapar el mismo hueco de categoría (VERSUS).

Mapeo a HUD: vidas = blancos no acertados permitidos antes de game over (empieza en 3, precedente de `rocas`); nivel = ronda/oleada, sube velocidad de cruce y densidad de blancos simultáneos (precedente de `rocas` derivando nivel de oleada limpiada); score = puntos por blanco acertado, con bonus por blancos más rápidos o más pequeños. Controles: solo mouse (clic para disparar sobre el blanco bajo el cursor), sin teclado necesario — mismo paradigma mouse-only de `bloque-buster`/`parejas`/`secuencia`. Galería se propone con id nuevo `galeria`.

Con esta propuesta el catálogo proyectado (11 candidatos de id nuevo/placeholder, ninguno aceptado aún, más los 8 originales) quedaría ARCADE 4, PUZZLE 4, SHOOTER 4 (`rocas`, `invasores`, `impacto`, `galeria`), VERSUS 3 — VERSUS pasa a ser la única categoría rezagada del proyectado.

**Candidatos agotados tras esta ronda:** los cuatro placeholders, `fusion` (2048), `estela` (Tron), `impacto` (Missile Command), `secuencia` (Simón), `nocaut` (Boxeo), `parejas` (Memorice) y ahora `galeria` (Galería) — **once propuestas en total**, faltan nueve para la meta de 20. Si se pide excluir `galeria` en una duodécima ronda, los respaldos son `Combate de tanques` o `Whack-a-mole` (ambos `considerado`); Match-3, Q\*bert y Centipede siguen descartados/considerados sin cambio de veredicto. Con VERSUS como única categoría en 3 proyectado, la próxima ronda debería priorizar un cuarto refuerzo de VERSUS (p. ej. retomar `Combate de tanques`) o aceptar que la variedad deje de discriminar del todo y el criterio pase a ser 100% esfuerzo/riesgo.

### Duodécima ronda 2026-07-31 (octavo id nuevo, distinto de los once ya propuestos)

Se pide un duodécimo juego con id nuevo, distinto de los once ya `propuesto` (cuatro placeholders + `fusion`, `estela`, `impacto`, `secuencia`, `nocaut`, `parejas`, `galeria`), pudiendo retomar `Centipede`/`Match-3`/`Q*bert`/`Combate de tanques`/`Whack-a-mole` desde `considerado` o traer una idea fresca. Se reconfirmó (`Glob` sobre `specs/**`, `references/started-games/**` y `references/source-assets/**`) que no hay specs nuevas (siguen 01–09) ni fuentes nuevas: las tres carpetas de `references/started-games/` siguen portadas y `source-assets/` solo tiene `snake-assets`, ya consumido — duodécimo candidato también es diseño desde cero con costo fijo de migración.

Catálogo **proyectado** (8 ids actuales + los once ya `propuesto`, ninguno aceptado/implementado todavía): ARCADE 4, PUZZLE 4, SHOOTER 4, VERSUS 3 (`duelo-pixel`, `estela`, `nocaut`). VERSUS sigue siendo la única categoría rezagada, tal como cerró la nota de la undécima ronda.

Siguiendo la sugerencia explícita de la nota anterior (variantes menos canónicas tipo Conecta 4/Reversi), se evaluó **Conecta 4** (grilla 7×6, el jugador y la CPU alternan turnos soltando fichas por columna, gana quien alinee 4 en cualquier dirección; IA mínima que revisa jugada ganadora propia, jugada de bloqueo del rival y si no hay ninguna, elige columna con heurística simple de centro) contra **Reversi/Otelo** (grilla 8×8, cada ficha colocada voltea las del rival encerradas en línea, gana quien tenga más fichas al llenar el tablero) y se reevaluó `Combate de tanques` por cuarta vez.

Conecta 4 gana: su lógica central (apilar ficha en la columna más baja libre + revisar 4-en-línea en las 4 direcciones desde la última ficha) es más simple y con menos casos límite que el volteo de Reversi (que exige recorrer 8 direcciones desde cada jugada y voltear todas las fichas encerradas, con más superficie para bugs de "movimiento inválido" cuando no hay flancos que voltear). Ambos son turn-based, sin física, sin colisión de sprites en tiempo real, con una IA mínima (greedy/heurística de 1-2 niveles, sin minimax profundo) — pero Conecta 4 es más barato de balancear. `Combate de tanques`, reevaluado por cuarta vez, se mantiene `considerado`: sigue exigiendo maze de obstáculos + rebote de balas + IA de puntería, más caro que cualquiera de las dos opciones de mesa.

Mapeo a HUD (el juego original no tiene ninguno de los tres conceptos, mismo caso de diseño que `parejas`/`secuencia`): vidas = partidas perdidas permitidas antes de game over, empieza en 3 (precedente `bloque-buster`/`rocas`), baja una si la CPU alinea 4 primero; nivel = ronda jugada contra la CPU, sube la profundidad de heurística de la IA (más agresiva bloqueando) cada ronda ganada (precedente `nocaut` derivando nivel de round); score = +10 por ficha colocada, +200 bonus por ronda ganada. Controles: clic en la columna sobre el tablero para soltar ficha, con fallback de flechas `←` `→` para mover un indicador y `Espacio`/`Enter` para confirmar — mismo paradigma mouse-first con fallback de teclado que `secuencia`. Conecta 4 se propone con id nuevo `conecta4`.

Con esta propuesta el catálogo proyectado (12 candidatos de id nuevo/placeholder, ninguno aceptado aún, más los 8 originales) quedaría ARCADE 4, PUZZLE 4, SHOOTER 4, VERSUS 4 (`duelo-pixel`, `estela`, `nocaut`, `conecta4`) — las cuatro categorías empatadas por primera vez.

**Candidatos agotados tras esta ronda:** los cuatro placeholders, `fusion` (2048), `estela` (Tron), `impacto` (Missile Command), `secuencia` (Simón), `nocaut` (Boxeo), `parejas` (Memorice), `galeria` (Galería) y ahora `conecta4` (Conecta 4) — **doce propuestas en total**, faltan ocho para la meta de 20. Si se pide excluir `conecta4` en una decimotercera ronda, los respaldos son `Reversi` (nuevo `considerado` de esta ronda) o `Combate de tanques`/`Whack-a-mole` (`considerado` de rondas previas); Match-3, Q\*bert y Centipede siguen descartados/considerados sin cambio de veredicto. Con las cuatro categorías empatadas en el proyectado, la variedad de catálogo deja de discriminar del todo a partir de la próxima ronda — el criterio pasará a ser 100% esfuerzo/riesgo, y conviene revisar si alguno de los doce candidatos `propuesto` puede aceptarse/implementarse antes de seguir sumando ideas cada vez más forzadas.

### Decimotercera ronda 2026-07-31 (noveno id nuevo, distinto de los doce ya propuestos; criterio 100% esfuerzo/riesgo)

Se pide un decimotercer juego con id nuevo, distinto de los doce ya `propuesto` (cuatro placeholders + `fusion`, `estela`, `impacto`, `secuencia`, `nocaut`, `parejas`, `galeria`, `conecta4`). Con las cuatro categorías empatadas en 4 tras la duodécima ronda (tal como anticipó su propia nota de cierre), el criterio deja de ser variedad de catálogo y pasa a ser **100% esfuerzo/riesgo**. Se reconfirmó (`Glob` sobre `specs/**`, `references/started-games/**` y `references/source-assets/**`) que no hay specs nuevas (siguen 01–09) ni fuentes nuevas: las tres carpetas de `references/started-games/` siguen portadas y `source-assets/` solo tiene `snake-assets`, ya consumido — decimotercer candidato también es diseño desde cero con costo fijo de migración.

Se revisaron los seis candidatos `considerado` acumulados (Centipede, Match-3, Q\*bert, Combate de tanques —reevaluado cuatro veces—, Whack-a-mole, Reversi) puramente por esfuerzo, sin ponderar categoría. Orden de esfuerzo estimado, de menor a mayor: **Whack-a-mole** (grilla fija de N agujeros con posiciones estáticas, un temporizador de aparición/ocultamiento por agujero, hit-test de clic contra el agujero activo — sin trayectorias que calcular, sin física, sin IA; más simple incluso que `galeria`, que necesita mover blancos en arcos/líneas) < **Reversi** (grilla 8×8, pero exige recorrer 8 direcciones desde cada jugada y voltear todas las fichas encerradas, con más casos límite de "sin flancos que voltear") < **Match-3** (detección de coincidencias + cascada de relleno + re-chequeo de combos + "sin movimientos posibles") < **Combate de tanques** (maze de obstáculos + rebote de balas en pared + IA de puntería) ≈ **Q\*bert** (paradigma isométrico atípico, más riesgo de render/input no probado en el catálogo) < **Centipede** (segmentos encadenados que se dividen + tres IAs distintas).

Whack-a-mole gana con claridad: es el candidato de menor esfuerzo/riesgo absoluto del pool completo acumulado en trece rondas, comparable o por debajo de `parejas`/`secuencia` en líneas de código estimadas. Mapeo a HUD (el juego original no tiene ninguno de los tres conceptos, mismo caso de diseño que `parejas`/`galeria`): vidas = topos escapados (no golpeados a tiempo) permitidos antes de game over, empieza en 3 (precedente `bloque-buster`/`rocas`/`galeria`); nivel = ronda, sube la frecuencia de aparición y baja el tiempo que cada topo queda visible (precedente `galeria` derivando nivel de oleada); score = puntos por golpe acertado, con bonus por golpes consecutivos sin fallo. Controles: solo mouse (clic sobre el agujero con el topo activo), mismo paradigma mouse-only que `bloque-buster`/`parejas`/`secuencia`/`galeria`. Whack-a-mole se propone con id nuevo `topos`.

`Reversi` queda `considerado` (reevaluada segunda vez): sigue siendo más cara que Whack-a-mole por el volteo en 8 direcciones. `Combate de tanques` queda `considerado` (reevaluada quinta vez): se mantiene como el más caro de los candidatos de VERSUS. Match-3, Q\*bert y Centipede no se reevaluaron explícitamente esta ronda por quedar claramente por encima de Whack-a-mole y Reversi en esfuerzo; conservan su veredicto previo.

Con esta propuesta el catálogo proyectado (13 candidatos de id nuevo/placeholder, ninguno aceptado aún, más los 8 originales) quedaría ARCADE 5 (`bloque-buster`, `serpentina`, `gloton`, `ranaria`, `topos`), PUZZLE 4, SHOOTER 4, VERSUS 4 — ARCADE vuelve a desbalancearse, pero es una consecuencia aceptada del cambio de criterio a esfuerzo/riesgo puro.

**Candidatos agotados tras esta ronda:** los cuatro placeholders, `fusion` (2048), `estela` (Tron), `impacto` (Missile Command), `secuencia` (Simón), `nocaut` (Boxeo), `parejas` (Memorice), `galeria` (Galería), `conecta4` (Conecta 4) y ahora `topos` (Whack-a-mole) — **trece propuestas en total**, faltan siete para la meta de 20. Si se pide excluir `topos` en una decimocuarta ronda, los respaldos en orden de esfuerzo son `Reversi`, luego `Match-3`, luego `Combate de tanques`/`Q*bert`, y por último `Centipede`. El pool de clásicos arcade/mesa de bajo riesgo está casi agotado; las próximas rondas probablemente tendrán que evaluar variantes de segundo grado (skins alternativos de mecánicas ya cubiertas, o clásicos menos conocidos) para llegar a la meta de 20.

### Decimocuarta ronda 2026-07-31 (décimo id nuevo, distinto de los trece ya propuestos; criterio 100% esfuerzo/riesgo)

Se pide un decimocuarto juego con id nuevo, distinto de los trece ya `propuesto` (cuatro placeholders + `fusion`, `estela`, `impacto`, `secuencia`, `nocaut`, `parejas`, `galeria`, `conecta4`, `topos`). Se reconfirmó (`Glob` sobre `specs/**` y `references/**/*`) que no hay specs nuevas (siguen 01–09) ni fuentes nuevas: las tres carpetas de `references/started-games/` siguen portadas y `source-assets/` solo tiene `snake-assets`, ya consumido — decimocuarto candidato también es diseño desde cero con costo fijo de migración. El criterio sigue siendo **100% esfuerzo/riesgo** (variedad de catálogo ya no discrimina desde la decimotercera ronda).

Se reevaluó `Reversi` (siguiente en la cola de respaldos por esfuerzo desde la ronda anterior) contra una idea fresca: **Buscaminas** (grilla oculta con minas colocadas al azar tras el primer clic para garantizar que este nunca detone una mina; clic izquierdo revela una celda, revelado en cascada — flood-fill iterativo, no recursivo, para evitar problemas de profundidad — sobre celdas vacías contiguas hasta topar con números; clic derecho marca/desmarca bandera; pisar una mina termina la partida). Buscaminas gana con margen claro: es el único candidato del pool acumulado en catorce rondas que **no requiere ninguna IA** (a diferencia de Reversi, Conecta 4 o Combate de tanques, todos con un oponente CPU que evaluar) ni un algoritmo de cascada con re-chequeo de combos propenso a bugs sutiles (a diferencia de Match-3) — es un juego de un solo jugador contra el tablero, con un algoritmo de flood-fill bien conocido y de bajo riesgo. `Reversi`, reevaluado por tercera vez, sigue exigiendo IA (aunque heurística simple) + volteo en 8 direcciones con casos límite de "sin flancos que voltear"; queda `considerado`, segunda. `Combate de tanques`, reevaluado por sexta vez, se mantiene como el más caro de todos los candidatos VERSUS acumulados; queda `considerado`, tercera.

Mapeo a HUD (el Buscaminas original no tiene ninguno de los tres conceptos nativamente, mismo caso de diseño que `caida`/`serpentina` fijando vidas ausentes): vidas = fijas en 1 — pisar una mina termina la partida al instante, sin margen de error, tal como el juego original; nivel = ronda superada al despejar el tablero por completo, sube tamaño de grilla y densidad de minas (p. ej. 9×9/10 minas → 12×12/20 → 16×16/40), precedente `caida`/`galeria` derivando nivel de progreso; score = +10 por celda revelada, +500 de bonus al despejar el tablero completo. Controles: solo mouse — clic izquierdo revela, clic derecho marca bandera (requiere `preventDefault` en el evento `contextmenu` del canvas). Buscaminas se propone con id nuevo `minas`.

Con esta propuesta el catálogo proyectado (14 candidatos de id nuevo/placeholder, ninguno aceptado aún, más los 8 originales) quedaría ARCADE 5, PUZZLE 5 (`caida`, `fusion`, `secuencia`, `parejas`, `minas`), SHOOTER 4, VERSUS 4 — PUZZLE alcanza a ARCADE como categoría más grande del proyectado, aunque esto ya no pesa en la decisión bajo el criterio actual.

**Candidatos agotados tras esta ronda:** los cuatro placeholders, `fusion` (2048), `estela` (Tron), `impacto` (Missile Command), `secuencia` (Simón), `nocaut` (Boxeo), `parejas` (Memorice), `galeria` (Galería), `conecta4` (Conecta 4), `topos` (Whack-a-mole) y ahora `minas` (Buscaminas) — **catorce propuestas en total**, faltan seis para la meta de 20. Si se pide excluir `minas` en una decimoquinta ronda, los respaldos en orden de esfuerzo son `Reversi`, luego `Match-3`, luego `Combate de tanques`/`Q*bert`, y por último `Centipede`. El pool de clásicos de bajo riesgo sin AI ni cascada está prácticamente agotado tras esta ronda; la próxima probablemente tendrá que aceptar un candidato con IA (Reversi) o buscar una variante de segundo grado genuinamente nueva (p. ej. Solitario/Klondike, Buscapalabras, o un shooter de galería con skin distinto).

### Decimoquinta ronda 2026-07-31 (undécimo id nuevo, distinto de los catorce ya propuestos; criterio 100% esfuerzo/riesgo)

Se pide un decimoquinto juego con id nuevo, distinto de los catorce ya `propuesto` (cuatro placeholders + `fusion`, `estela`, `impacto`, `secuencia`, `nocaut`, `parejas`, `galeria`, `conecta4`, `topos`, `minas`). Se reconfirmó (`Glob` sobre `specs/**` y `references/**/*`) que no hay specs nuevas (siguen 01–09) ni fuentes nuevas: las tres carpetas de `references/started-games/` siguen portadas y `source-assets/` solo tiene `snake-assets`, ya consumido — decimoquinto candidato también es diseño desde cero con costo fijo de migración. El criterio sigue siendo **100% esfuerzo/riesgo**.

Siguiendo la sugerencia de la nota anterior, se evaluaron dos ideas frescas — **Solitario (Klondike)** y **Buscapalabras** — más una tercera no mencionada antes: un **juego estilo Flappy Bird** (un ave/nave que cae por gravedad constante, un solo botón/tecla para impulsarla hacia arriba, tuberías que se desplazan de derecha a izquierda con un hueco de paso; chocar contra una tubería o el suelo termina la partida al instante; cada tubería cruzada suma puntos y sube ligeramente la velocidad de desplazamiento). El estilo Flappy gana con claridad sobre los dos respaldos sugeridos: no necesita ninguna IA (a diferencia de Reversi/Combate de tanques/Conecta 4), ningún algoritmo de cascada o re-chequeo (a diferencia de Match-3), ningún motor de reglas de pilas con validación de movimientos (Solitario: fundación ascendente por palo, tablero descendente alternando color, reyes en columna vacía, traslado de secuencias completas) y ningún generador de colocación de palabras con backtracking en 8 direcciones (Buscapalabras) — es solo un objeto con gravedad constante, un impulso vertical al pulsar, y colisión AABB contra un par de rectángulos por tubería. Esfuerzo estimado por debajo de `serpentina` (258 líneas): no hay grilla, no hay cola que crece, no hay multi-entidad más allá de las tuberías en pantalla.

Mapeo a HUD (el juego original no tiene concepto de vidas ni nivel numerado, mismo caso de diseño que `caida`/`serpentina` fijando valores ausentes): vidas = fijas en 1 — cualquier colisión termina la partida al instante, igual que Buscaminas; nivel = `floor(tuberías cruzadas / 10) + 1`, sube la velocidad de scroll y achica ligeramente el hueco de paso (precedente `serpentina` derivando nivel de progreso vía frutas); score = +10 por cada tubería cruzada. Controles: `Espacio` o clic del mouse para impulsar hacia arriba, sin más input — el control de un solo botón más simple del catálogo hasta ahora. Flappy Bird (estilo) se propone con id nuevo `vuelo`.

`Solitario (Klondike)` queda `considerado`: sus reglas de movimiento entre pilas son las más numerosas y propensas a bugs de validación del lote evaluado esta ronda. `Buscapalabras` queda `considerado`: su algoritmo de colocación de palabras con backtracking para evitar conflictos de letras es más arriesgado que la física simple de `vuelo`. `Reversi`, `Match-3`, `Combate de tanques`, `Q*bert` y `Centipede` no se reevaluaron explícitamente esta ronda por quedar claramente por encima de `vuelo` en esfuerzo; conservan su veredicto previo.

Con esta propuesta el catálogo proyectado (15 candidatos de id nuevo/placeholder, ninguno aceptado aún, más los 8 originales) quedaría ARCADE 6 (`bloque-buster`, `serpentina`, `gloton`, `ranaria`, `topos`, `vuelo`), PUZZLE 5, SHOOTER 4, VERSUS 4 — el desbalance de ARCADE se profundiza, consecuencia aceptada del criterio 100% esfuerzo/riesgo.

**Candidatos agotados tras esta ronda:** los cuatro placeholders, `fusion` (2048), `estela` (Tron), `impacto` (Missile Command), `secuencia` (Simón), `nocaut` (Boxeo), `parejas` (Memorice), `galeria` (Galería), `conecta4` (Conecta 4), `topos` (Whack-a-mole), `minas` (Buscaminas) y ahora `vuelo` (Flappy Bird) — **quince propuestas en total**, faltan cinco para la meta de 20. Si se pide excluir `vuelo` en una decimosexta ronda, los respaldos en orden de esfuerzo son `Solitario (Klondike)` o `Buscapalabras` (ambos `considerado`, nuevos esta ronda), luego `Reversi`, luego `Match-3`, luego `Combate de tanques`/`Q*bert`, y por último `Centipede`. El pool de clásicos de un solo jugador sin IA y sin cascada está prácticamente agotado tras esta ronda; conviene revisar si alguno de los quince candidatos `propuesto` puede aceptarse/implementarse antes de seguir generando ideas cada vez más forzadas.

### Decimosexta ronda 2026-07-31 (duodécimo id nuevo, distinto de los quince ya propuestos; criterio 100% esfuerzo/riesgo)

Se pide un decimosexto juego con id nuevo, distinto de los quince ya `propuesto` (cuatro placeholders + `fusion`, `estela`, `impacto`, `secuencia`, `nocaut`, `parejas`, `galeria`, `conecta4`, `topos`, `minas`, `vuelo`). Se reconfirmó (`Glob` sobre `specs/**`, `references/started-games/**` y `references/source-assets/**`) que no hay specs nuevas (siguen 01–09) ni fuentes nuevas: las tres carpetas de `references/started-games/` siguen portadas y `source-assets/` solo tiene `snake-assets`, ya consumido — decimosexto candidato también es diseño desde cero con costo fijo de migración. El criterio sigue siendo **100% esfuerzo/riesgo**.

Se reevaluó la cola de respaldos anotada al cierre de la decimoquinta ronda (`Solitario`, `Buscapalabras`, `Reversi`, en ese orden de esfuerzo estimado) contra una idea fresca no evaluada antes: **Tres en raya / Gato** (tablero 3×3, dos jugadores alternan turno colocando ficha en una celda vacía, gana quien alinee 3 en cualquier fila/columna/diagonal, empate si se llena el tablero sin ganador; IA heurística de tres reglas en cascada — ganar si hay jugada ganadora disponible, bloquear si el rival tiene una, si no elegir centro o esquina libre — sin necesidad de minimax ni búsqueda de árbol, dado el tamaño trivial del espacio de estados).

Gato gana con claridad sobre los tres respaldos: es un tablero de 9 celdas con un chequeo de victoria de solo 8 líneas fijas (3 filas + 3 columnas + 2 diagonales), sin gravedad de columna, sin volteo de fichas, sin gestión de pilas ni recirculación de mazo. Comparado con `Solitario` (7 columnas de tablero + 4 fundaciones + pila de robo/descarte + validación de secuencias completas al mover, mucho más estado y casos límite pese a no tener IA), `Buscapalabras` (algoritmo de colocación de palabras con backtracking en 8 direcciones, riesgo de generación) y `Reversi` (tablero 8×8, volteo en 8 direcciones por jugada, IA con paso de turno obligado si no hay jugada válida), Gato tiene la menor superficie de código y de bugs de todo el pool acumulado en dieciséis rondas — de hecho menos que el propio `conecta4` (sin gravedad de columna que calcular, board 9 celdas en vez de 42).

Mapeo a HUD (el juego original no tiene ninguno de los tres conceptos nativamente, mismo caso de diseño que `nocaut`/`conecta4`): vidas = partidas perdidas permitidas antes de game over, empieza en 3 (precedente `bloque-buster`/`rocas`/`nocaut`/`conecta4`); nivel = ronda jugada contra la CPU, sube la profundidad de la heurística (de solo "bloquear si hay amenaza" a "ganar > bloquear > centro > esquina" completo) cada ronda ganada, precedente `nocaut`/`conecta4` derivando nivel de round; score = +100 por partida ganada, +20 por empate, +0 por derrota (resta una vida). Controles: clic en la celda del tablero 3×3, con fallback de flechas `↑` `↓` `←` `→` para mover un indicador entre celdas y `Espacio`/`Enter` para confirmar — mismo paradigma mouse-first con fallback de teclado que `conecta4`/`secuencia`.

`Solitario (Klondike)` queda `considerado` (reevaluado en esta ronda como cabeza de cola): sin IA, pero su gestión de múltiples pilas y traslado de secuencias completas exige más estado y validación que un tablero 3×3; queda segunda. `Reversi` queda `considerado` (reevaluado por cuarta vez): sigue siendo más caro por el tablero 8×8 y el volteo en 8 direcciones; queda tercera. `Buscapalabras`, `Match-3`, `Combate de tanques`, `Q*bert` y `Centipede` no se reevaluaron explícitamente por quedar claramente por encima de Gato en esfuerzo; conservan su veredicto previo. Gato se propone con id nuevo `gato`.

**Riesgo de diseño anotado explícitamente:** Gato profundiza el mismo patrón de "juego de mesa por turnos con IA mínima" que ya cubren `conecta4` y (en cola) `Reversi` — mecánicamente distinto en el input (colocación libre en cualquier celda vacía vs. caída por gravedad en una columna), pero de la misma familia conceptual. Bajo el criterio actual (100% esfuerzo/riesgo, la variedad de catálogo ya no discrimina desde la decimotercera ronda) esto no penaliza la elección, pero se deja constancia para que una futura ronda no lo repropong a ciegas como si fuera una categoría de mecánica distinta a `conecta4`.

Con esta propuesta el catálogo proyectado (16 candidatos de id nuevo/placeholder, ninguno aceptado aún, más los 8 originales) quedaría ARCADE 6, PUZZLE 5, SHOOTER 4, VERSUS 5 (`duelo-pixel`, `estela`, `nocaut`, `conecta4`, `gato`) — dato informativo, no discriminante bajo el criterio actual.

**Candidatos agotados tras esta ronda:** los cuatro placeholders, `fusion` (2048), `estela` (Tron), `impacto` (Missile Command), `secuencia` (Simón), `nocaut` (Boxeo), `parejas` (Memorice), `galeria` (Galería), `conecta4` (Conecta 4), `topos` (Whack-a-mole), `minas` (Buscaminas), `vuelo` (Flappy Bird) y ahora `gato` (Tres en raya) — **dieciséis propuestas en total**, faltan cuatro para la meta de 20. Si se pide excluir `gato` en una decimoséptima ronda, los respaldos en orden de esfuerzo son `Solitario (Klondike)`, luego `Reversi`, luego `Buscapalabras`, luego `Match-3`, luego `Combate de tanques`/`Q*bert`, y por último `Centipede`. El pool de clásicos de bajo riesgo genuinamente nuevos está casi agotado; las próximas cuatro rondas probablemente tendrán que aceptar candidatos con IA más compleja (Reversi) o algoritmos de generación/cascada (Buscapalabras, Match-3), o buscar variantes de segundo grado (p. ej. Damas, Ahorcado, un segundo shooter de galería con skin distinto).

### Decimoséptima ronda 2026-07-31 (decimotercer id nuevo, distinto de los dieciséis ya propuestos; criterio 100% esfuerzo/riesgo)

Se pide un decimoséptimo juego con id nuevo, distinto de los dieciséis ya `propuesto` (cuatro placeholders + `fusion`, `estela`, `impacto`, `secuencia`, `nocaut`, `parejas`, `galeria`, `conecta4`, `topos`, `minas`, `vuelo`, `gato`). Se reconfirmó (`Glob` sobre `specs/**`, `references/started-games/**` y `references/source-assets/**`) que no hay specs nuevas (siguen 01–09) ni fuentes nuevas: las tres carpetas de `references/started-games/` siguen portadas y `source-assets/` solo tiene `snake-assets`, ya consumido — decimoséptimo candidato también es diseño desde cero con costo fijo de migración. El criterio sigue siendo **100% esfuerzo/riesgo**.

Se reevaluó `Solitario (Klondike)` (cabeza de la cola de respaldos desde la nota de cierre anterior) contra dos ideas frescas: **Lights Out** (grid de N×N luces; clic en una celda alterna esa luz y sus vecinas ortogonales; objetivo, apagar todas; generación trivialmente solvable partiendo del estado resuelto y aplicando movimientos válidos al azar, sin necesidad de verificar solvabilidad después) y **Ahorcado / Hangman** (se elige una palabra al azar de una lista fija; el jugador adivina letras por teclado; cada letra incorrecta resta un intento y dibuja una etapa del muñeco; adivinar todas las letras antes de agotar los intentos gana la ronda).

Ahorcado gana con claridad sobre los otros dos: no requiere ninguna estructura de grilla (a diferencia de Lights Out, que aunque no necesita IA sí necesita lógica de propagación de toggle sobre vecinos y su representación en canvas), ningún algoritmo de generación con invariantes que verificar (a diferencia de un sliding-puzzle clásico, descartado de plano por el chequeo de paridad necesario para garantizar una mezcla resoluble), y ninguna gestión de pilas/secuencias como Solitario. Su modelo de datos es el más simple del pool acumulado en diecisiete rondas: una palabra de un array fijo, un `Set` de letras adivinadas, un contador de fallos. Sin IA, sin cascada, sin colisión de sprites, sin física. Es, además, el primer candidato genuinamente **de teclado puro** desde `secuencia` (los últimos seis candidatos — `parejas`, `galeria`, `conecta4`, `topos`, `minas`, `gato` — son todos mouse-first), lo que de paso reduce el riesgo de reproducir el mismo paradigma de input una séptima vez.

Mapeo a HUD (el Ahorcado clásico ya trae un concepto de vidas nativo, a diferencia de la mayoría de los últimos candidatos): vidas = intentos fallidos permitidos antes de completar el muñeco (empieza en 6, el número canónico del juego, cada etapa del dibujo = 1 vida perdida); nivel = ronda/palabra, sube la longitud o rareza de la palabra siguiente (precedente `caida`/`secuencia` derivando nivel de progreso); score = +10 por letra correcta única, +100 de bonus por completar la palabra, con bonus extra por menos fallos. Controles: teclado físico A-Z (con fallback de clic en un teclado en pantalla dibujado en el propio canvas, mismo patrón de fallback que `conecta4`/`gato`). Categoría: `PUZZLE` (evita abrir una categoría nueva en `CATS`, sin impacto en el criterio de esta ronda pero sin costo adicional de migración de UI). Ahorcado se propone con id nuevo `verdugo`.

`Solitario (Klondike)` queda `considerado` (reevaluado por tercera vez): sin IA, pero su gestión de 7 columnas + 4 fundaciones + robo/reciclado de mazo + traslado de secuencias completas sigue exigiendo bastante más estado y validación que el modelo trivial de Ahorcado; queda segunda. `Lights Out` es idea fresca de esta ronda, queda `considerado`: sin IA y con generación trivialmente solvable, pero su lógica de propagación de toggle sobre una grilla (y su representación visual en canvas) es más superficie de código que Ahorcado; queda tercera. `Reversi`, `Buscapalabras`, `Match-3`, `Combate de tanques`, `Q*bert` y `Centipede` no se reevaluaron explícitamente por quedar claramente por encima de Ahorcado en esfuerzo; conservan su veredicto previo.

Con esta propuesta el catálogo proyectado (17 candidatos de id nuevo/placeholder, ninguno aceptado aún, más los 8 originales) quedaría ARCADE 6, PUZZLE 6 (`caida`, `fusion`, `secuencia`, `parejas`, `minas`, `verdugo`), SHOOTER 4, VERSUS 5 — dato informativo, no discriminante bajo el criterio actual.

**Candidatos agotados tras esta ronda:** los cuatro placeholders, `fusion` (2048), `estela` (Tron), `impacto` (Missile Command), `secuencia` (Simón), `nocaut` (Boxeo), `parejas` (Memorice), `galeria` (Galería), `conecta4` (Conecta 4), `topos` (Whack-a-mole), `minas` (Buscaminas), `vuelo` (Flappy Bird), `gato` (Tres en raya) y ahora `verdugo` (Ahorcado) — **diecisiete propuestas en total**, faltan tres para la meta de 20. Si se pide excluir `verdugo` en una decimoctava ronda, los respaldos en orden de esfuerzo son `Solitario (Klondike)`, luego `Lights Out`, luego `Reversi`, luego `Buscapalabras`, luego `Match-3`, luego `Combate de tanques`/`Q*bert`, y por último `Centipede`. El pool de mecánicas de un solo jugador sin IA y sin cascada está prácticamente agotado; las próximas rondas casi con seguridad tendrán que aceptar un candidato con IA (Reversi) o buscar una variante de segundo grado (p. ej. Damas, Sudoku, un segundo sliding-puzzle con verificación de paridad, o un shooter/arcade con skin distinto).

### Decimoctava ronda 2026-07-31 (decimocuarto id nuevo, distinto de los diecisiete ya propuestos; criterio 100% esfuerzo/riesgo)

Se pide un decimoctavo juego con id nuevo, distinto de los diecisiete ya `propuesto` (cuatro placeholders + `fusion`, `estela`, `impacto`, `secuencia`, `nocaut`, `parejas`, `galeria`, `conecta4`, `topos`, `minas`, `vuelo`, `gato`, `verdugo`). Se reconfirmó (`Glob` sobre `specs/**`, `references/started-games/**` y `references/source-assets/**`) que no hay specs nuevas (siguen 01–09) ni fuentes nuevas: las tres carpetas de `references/started-games/` siguen portadas y `source-assets/` solo tiene `snake-assets`, ya consumido — decimoctavo candidato también es diseño desde cero con costo fijo de migración. El criterio sigue siendo **100% esfuerzo/riesgo**.

Se reevaluó `Solitario (Klondike)` (cabeza de la cola de respaldos desde el cierre de la decimoséptima ronda) contra dos ideas frescas: **Nim** (varias pilas de fichas; en cada turno el jugador o la CPU retira 1 o más fichas de una sola pila; quien retira la última ficha gana; la CPU juega de forma óptima calculando el nim-sum — XOR bit a bit de los tamaños de todas las pilas — y moviendo a una posición con nim-sum cero cuando existe, una fórmula cerrada y determinista, sin heurística ni búsqueda de árbol) y **Mastermind / código secreto** (se genera un código aleatorio de N posiciones con colores de un set fijo, permitiendo repetidos; el jugador arma un intento eligiendo colores por clic y lo envía; el juego devuelve retroalimentación de aciertos exactos —color y posición— y aciertos de color en posición incorrecta, calculada con un conteo cuidadoso que no reutiliza la misma clavija dos veces; el jugador dispone de un número fijo de intentos para descifrar el código).

Mastermind gana con el margen más claro de todo el historial: a diferencia de Nim, Reversi, Conecta 4, Combate de tanques o el propio Gato, **no necesita ningún oponente que evaluar** — no hay turnos alternados, no hay IA de ningún tipo (ni heurística de reglas en cascada como Gato, ni fórmula matemática como el nim-sum de Nim), solo un secreto generado una vez al azar y una función de comparación entre dos arreglos de longitud fija. Tampoco necesita grilla con propagación (a diferencia de Lights Out), ni gestión de pilas (a diferencia de Solitario), ni generación con backtracking (a diferencia de Buscapalabras), ni cascada de combos (a diferencia de Match-3). Su superficie de código es, de hecho, menor que la del propio Ahorcado (`verdugo`): no hace falta lista de palabras ni dibujo progresivo del muñeco, solo un arreglo de colores objetivo y un bucle de conteo de coincidencias.

Nim queda segundo, muy cerca: su IA es una fórmula determinista y probadamente óptima (no un heurístico ad-hoc), lo que la hace de bajo riesgo, pero sigue exigiendo turnos alternados jugador/CPU y un chequeo de estado terminal (todas las pilas en cero) que Mastermind no necesita, al no haber alternancia de turnos.

Mapeo a HUD (el Mastermind clásico ya trae un concepto de intentos limitados, similar al caso de `verdugo`): vidas = intentos restantes antes de game over, empieza en 10 (el número canónico del juego), baja uno por cada intento que no sea coincidencia exacta total; nivel = código descifrado (ronda), sube la longitud del código o la cantidad de colores disponibles en el set cada ronda ganada (precedente `caida`/`parejas` derivando nivel de progreso); score = +100 por código descifrado, con bonus por intentos restantes al acertar (precedente `verdugo` bonificando por menos fallos). Controles: mouse — clic en una paleta de colores fija para llenar cada posición del intento, clic en "enviar" para confirmarlo; fallback de teclado con teclas numéricas 1–6 para seleccionar color y `Enter` para enviar, mismo patrón de fallback que `conecta4`/`gato`. Categoría: `PUZZLE` (no requiere categoría nueva en `CATS`). Mastermind se propone con id nuevo `codigo`.

`Nim` es idea fresca de esta ronda, queda `considerado`: IA por fórmula (sin heurística ni riesgo de bugs de "regla mal priorizada"), pero exige turnos alternados y chequeo de pilas vacías que Mastermind no necesita; queda segunda. `Solitario (Klondike)` queda `considerado` (reevaluado por cuarta vez): sigue exigiendo la gestión de 7 columnas + 4 fundaciones + robo/reciclado de mazo + traslado de secuencias completas, muy por encima en estado y validación de cualquier candidato de esta ronda; queda tercera. `Lights Out`, `Reversi`, `Buscapalabras`, `Match-3`, `Combate de tanques`, `Q*bert` y `Centipede` no se reevaluaron explícitamente por quedar claramente por encima de Mastermind en esfuerzo; conservan su veredicto previo.

**Riesgo de diseño anotado explícitamente:** el algoritmo de retroalimentación de Mastermind (contar aciertos exactos primero, luego contar aciertos de color sin reutilizar clavijas ya usadas por un acierto exacto o por otro acierto de color) es un clásico foco de bugs sutiles de off-by-one/doble conteo cuando hay colores repetidos en el secreto o en el intento; aunque el algoritmo en sí es simple y bien documentado, conviene que el spec deje explícito el caso de prueba con duplicados para que `/spec-impl` no lo pase por alto.

Con esta propuesta el catálogo proyectado (18 candidatos de id nuevo/placeholder, ninguno aceptado aún, más los 8 originales) quedaría ARCADE 6, PUZZLE 7 (`caida`, `fusion`, `secuencia`, `parejas`, `minas`, `verdugo`, `codigo`), SHOOTER 4, VERSUS 5 — dato informativo, no discriminante bajo el criterio actual; PUZZLE pasa a ser la categoría más grande del proyectado.

**Candidatos agotados tras esta ronda:** los cuatro placeholders, `fusion` (2048), `estela` (Tron), `impacto` (Missile Command), `secuencia` (Simón), `nocaut` (Boxeo), `parejas` (Memorice), `galeria` (Galería), `conecta4` (Conecta 4), `topos` (Whack-a-mole), `minas` (Buscaminas), `vuelo` (Flappy Bird), `gato` (Tres en raya), `verdugo` (Ahorcado) y ahora `codigo` (Mastermind) — **dieciocho propuestas en total**, faltan dos para la meta de 20. Si se pide excluir `codigo` en una decimonovena ronda, los respaldos en orden de esfuerzo son `Nim` (nuevo `considerado` de esta ronda), luego `Solitario (Klondike)`, luego `Lights Out`, luego `Reversi`, luego `Buscapalabras`, luego `Match-3`, luego `Combate de tanques`/`Q*bert`, y por último `Centipede`. El pool de mecánicas genuinamente nuevas de bajo riesgo está casi agotado; las próximas dos rondas probablemente tendrán que aceptar a `Nim` (IA por fórmula, sin heurística ad-hoc, sigue siendo bajo riesgo pese a los turnos alternados) o un candidato con IA heurística más compleja (Reversi), o buscar una variante de segundo grado (Damas, Sudoku, un segundo sliding-puzzle con verificación de paridad).

### Decimonovena ronda 2026-07-31 (decimoquinto id nuevo, distinto de los dieciocho ya propuestos; criterio 100% esfuerzo/riesgo)

Se pide un decimonoveno juego con id nuevo, distinto de los dieciocho ya `propuesto` (cuatro placeholders + `fusion`, `estela`, `impacto`, `secuencia`, `nocaut`, `parejas`, `galeria`, `conecta4`, `topos`, `minas`, `vuelo`, `gato`, `verdugo`, `codigo`). Se reconfirmó (`Glob`/lectura de `specs/**`, `references/started-games/**` y `references/source-assets/**`) que no hay specs nuevas (siguen 01–09) ni fuentes nuevas: las tres carpetas de `references/started-games/` siguen portadas y `source-assets/` solo tiene `snake-assets`, ya consumido — decimonoveno candidato también es diseño desde cero con costo fijo de migración. El criterio sigue siendo **100% esfuerzo/riesgo**.

Se retomó `Nim` (cabeza de la cola de respaldos desde el cierre de la decimoctava ronda) y se lo comparó contra una idea fresca: **Damas (Checkers)** (tablero 8×8 con 12 fichas por bando, movimiento diagonal, captura obligatoria saltando sobre la ficha rival, coronación a "dama" al llegar a la última fila con movimiento en ambas diagonales, IA que evalúa capturas obligatorias y saltos múltiples encadenados). Damas es sensiblemente más cara: exige detectar todas las capturas obligatorias disponibles antes de permitir un movimiento simple, encadenar saltos múltiples en un mismo turno, y una IA que al menos evalúe 1-2 niveles de captura — más superficie de reglas y casos límite que cualquier candidato de mesa ya evaluado (`conecta4`, `gato`), y sin ninguna ventaja de esfuerzo sobre `Nim`.

`Nim` gana con claridad: sobre un conjunto de N pilas de fichas (tamaños iniciales aleatorios dentro de un rango), el jugador y la CPU alternan turnos retirando 1 o más fichas de una sola pila elegida; quien retira la última ficha gana la ronda. La IA de la CPU no es una heurística ad-hoc como en `conecta4`/`gato` (con su cascada de reglas "ganar > bloquear > centro > esquina"), sino una **fórmula cerrada y determinista**: el nim-sum (XOR bit a bit de los tamaños de todas las pilas). Si el nim-sum es distinto de cero, existe siempre un movimiento hacia una posición con nim-sum cero, calculable en una sola pasada sin backtracking ni evaluación de árbol; si es cero, cualquier movimiento sirve como intento (la CPU está en desventaja teórica). Esto hace que la IA de `Nim` sea, paradójicamente, la de **menor riesgo de bugs de todo el pool acumulado en diecinueve rondas** frente a cualquier oponente CPU: no hay ambigüedad de "qué regla aplicar primero", solo una operación aritmética. Comparado con `Solitario` (gestión de 7 columnas + 4 fundaciones + robo/reciclado de mazo, sin IA pero con mucho más estado y validación de movimientos), `Nim` no tiene pilas de cartas, ni fundaciones, ni secuencias que trasladar — solo N arreglos de enteros y un contador de fichas.

Mapeo a HUD (el Nim clásico no tiene ninguno de los tres conceptos nativamente, mismo caso de diseño que `conecta4`/`gato`/`codigo`): vidas = partidas perdidas permitidas antes de game over, empieza en 3 (precedente `bloque-buster`/`rocas`/`nocaut`/`conecta4`/`gato`); nivel = ronda jugada contra la CPU, sube el número de pilas (3 → 4 → 5) y el rango de fichas por pila cada ronda ganada (precedente `nocaut`/`conecta4`/`gato` derivando nivel de round); score = +50 por cada ficha retirada en un turno propio válido, +200 de bonus por ronda ganada. Controles: clic en una pila para seleccionarla y clic repetido (o arrastre) sobre las fichas de esa pila para elegir cuántas retirar, con un botón "confirmar" en pantalla; fallback de teclado con flechas `←` `→` para elegir pila, `↑` `↓` para ajustar la cantidad a retirar y `Enter` para confirmar — mismo patrón de fallback que `conecta4`/`gato`. Categoría: `VERSUS` (mismo criterio que `conecta4`/`gato`: juego de mesa por turnos contra la CPU). Nim se propone con id nuevo `montones`.

`Damas (Checkers)` es idea fresca de esta ronda, queda `considerado`: captura obligatoria + saltos múltiples encadenados + coronación es notablemente más caro que la fórmula de nim-sum; queda segunda. `Solitario (Klondike)`, reevaluado por quinta vez, se mantiene `considerado`: sigue exigiendo más estado y validación que Nim; queda tercera. `Lights Out`, `Reversi`, `Buscapalabras`, `Match-3`, `Combate de tanques`, `Q*bert` y `Centipede` no se reevaluaron explícitamente por quedar claramente por encima de Nim en esfuerzo; conservan su veredicto previo.

**Riesgo de diseño anotado explícitamente:** igual que con `conecta4`/`gato`, `Nim` profundiza el mismo patrón de "juego de mesa por turnos contra CPU" en VERSUS — mecánicamente distinto (retiro de fichas de pilas vs. colocación en grilla), pero de la misma familia conceptual amplia. Además, con una CPU jugando de forma matemáticamente óptima (nim-sum), el juego puede sentirse "imbatible" para el jugador salvo que la CPU empiece en desventaja o se le agregue una dificultad reducida (p. ej. que en niveles bajos la CPU no siempre juegue el movimiento óptimo) — esto es una decisión de diseño de balance a resolver en el spec, no un riesgo técnico de implementación.

Con esta propuesta el catálogo proyectado (19 candidatos de id nuevo/placeholder, ninguno aceptado aún, más los 8 originales) quedaría ARCADE 6, PUZZLE 7, SHOOTER 4, VERSUS 6 (`duelo-pixel`, `estela`, `nocaut`, `conecta4`, `gato`, `montones`) — dato informativo, no discriminante bajo el criterio actual.

**Candidatos agotados tras esta ronda:** los cuatro placeholders, `fusion` (2048), `estela` (Tron), `impacto` (Missile Command), `secuencia` (Simón), `nocaut` (Boxeo), `parejas` (Memorice), `galeria` (Galería), `conecta4` (Conecta 4), `topos` (Whack-a-mole), `minas` (Buscaminas), `vuelo` (Flappy Bird), `gato` (Tres en raya), `verdugo` (Ahorcado), `codigo` (Mastermind) y ahora `montones` (Nim) — **diecinueve propuestas en total**, falta solo una para la meta de 20 acordada con el usuario. Si se pide excluir `montones` en una vigésima ronda, los respaldos en orden de esfuerzo son `Damas (Checkers)` (nuevo `considerado` de esta ronda), luego `Solitario (Klondike)`, luego `Lights Out`, luego `Reversi`, luego `Buscapalabras`, luego `Match-3`, luego `Combate de tanques`/`Q*bert`, y por último `Centipede`. El pool de mecánicas de bajo riesgo genuinamente nuevas está agotado; la vigésima y última ronda de esta meta con toda probabilidad tendrá que aceptar un candidato con IA heurística más compleja (Reversi) o un algoritmo de generación/cascada (Lights Out, Buscapalabras, Match-3), o cerrar la meta reconociendo explícitamente que el pool de ideas de bajo riesgo se agotó.

### Vigésima ronda (final) 2026-07-31 (decimosexto id nuevo, distinto de los diecinueve ya propuestos; criterio 100% esfuerzo/riesgo — ronda de cierre de la tanda de 20 acordada con el usuario)

Se pide el vigésimo y último juego de la tanda, con id nuevo, distinto de los diecinueve ya `propuesto` (cuatro placeholders + `fusion`, `estela`, `impacto`, `secuencia`, `nocaut`, `parejas`, `galeria`, `conecta4`, `topos`, `minas`, `vuelo`, `gato`, `verdugo`, `codigo`, `montones`). Se reconfirmó (`Glob` sobre `specs/**`, `references/started-games/**` y `references/source-assets/**`, más `lib/data.ts`) que no hay specs nuevas (siguen 01–09), ni fuentes nuevas (`references/started-games/` con las tres carpetas ya portadas, `source-assets/` solo con `snake-assets` ya consumido), ni cambios en los 8 ids de `lib/data.ts` — vigésimo candidato también es diseño desde cero con costo fijo de migración. El criterio sigue siendo **100% esfuerzo/riesgo**.

Se retomó `Lights Out` (cabeza de la cola de respaldos por esfuerzo desde el cierre de la decimonovena ronda) y se lo reevaluó explícitamente contra los dos siguientes en esa misma cola, `Solitario (Klondike)` y `Reversi`. Lights Out gana con claridad como cierre de la tanda: de los ocho candidatos `considerado` acumulados en veinte rondas (Damas, Solitario, Lights Out, Reversi, Buscapalabras, Match-3, Combate de tanques, Q\*bert, Centipede), es el único que **no exige ningún oponente CPU** que evaluar (a diferencia de Reversi/Damas/Conecta4/Gato/Montones/Combate de tanques, todos con IA de algún tipo — heurística, fórmula cerrada o evaluación de puntería) ni una gestión extensa de estado sin IA (a diferencia de Solitario: 7 columnas + 4 fundaciones + robo/reciclado de mazo + traslado de secuencias) ni un algoritmo de generación con backtracking (Buscapalabras) o de cascada con re-chequeo de combos (Match-3). Su núcleo es una función de toggle sobre una grilla N×N: clic en una celda invierte esa celda y sus vecinas ortogonales (arriba/abajo/izquierda/derecha, sin wraparound); el objetivo es apagar todas las luces. La generación es trivialmente solvable partiendo del tablero resuelto (todo apagado) y aplicando K movimientos válidos al azar hacia atrás desde ahí — sin necesidad de verificar solvabilidad ni de chequeo de paridad, a diferencia de un sliding-puzzle clásico (descartado de plano por esa misma razón en la decimoséptima ronda).

Mapeo a HUD (el Lights Out clásico no tiene condición de derrota nativa — solo se resuelve o no se resuelve —, así que además de fijar valores ausentes, como en `conecta4`/`gato`/`montones`, aquí hay que inventar también el concepto de derrota): vidas = empieza en 3 (precedente `bloque-buster`/`rocas`/`nocaut`/`conecta4`/`gato`/`montones`), se resta una si el jugador agota un presupuesto de movimientos (K movimientos de la generación + un margen fijo, p. ej. +5) sin lograr apagar todas las luces; nivel = tablero resuelto (ronda), sube el tamaño de la grilla cada ronda ganada (3×3 → 4×4 → 5×5 → 6×6, precedente `caida`/`minas` derivando nivel de progreso vía tamaño/dificultad creciente); score = +50 por tablero resuelto, con bonus por eficiencia (menos clics usados que movimientos de generación, precedente `verdugo` bonificando por menos fallos). Controles: solo mouse — clic en una celda para alternarla junto con sus vecinas ortogonales, mismo paradigma mouse-only que `bloque-buster`/`parejas`/`galeria`/`topos`. Categoría: `PUZZLE` (no requiere categoría nueva en `CATS`). Lights Out se propone con id nuevo `apagon`.

`Solitario (Klondike)`, reevaluado por sexta vez, se mantiene `considerado`: sin IA, pero su gestión de 7 columnas + 4 fundaciones + robo/reciclado de mazo + traslado de secuencias completas exige mucho más estado y validación que una simple función de toggle; queda segunda. `Reversi`, reevaluado por quinta vez, se mantiene `considerado`: sigue exigiendo IA (heurística de flancos) + volteo en 8 direcciones con casos límite de "sin flancos que voltear" y paso de turno obligado; queda tercera. `Damas`, `Buscapalabras`, `Match-3`, `Combate de tanques`, `Q*bert` y `Centipede` no se reevaluaron explícitamente esta ronda por quedar claramente por encima de Lights Out en esfuerzo; conservan su veredicto previo sin cambios.

Con esta propuesta el catálogo proyectado (20 candidatos de id nuevo/placeholder, ninguno aceptado aún, más los 8 originales) quedaría ARCADE 6, PUZZLE 8 (`caida`, `fusion`, `secuencia`, `parejas`, `minas`, `verdugo`, `codigo`, `apagon`), SHOOTER 4, VERSUS 6 — dato informativo, no discriminante bajo el criterio actual.

**Cierre de la tanda de 20 propuestas.** Con `apagon` (Lights Out) se completan las veinte propuestas de id nuevo/placeholder acordadas con el usuario en esta serie de rondas: cuatro placeholders reutilizados (Pong/`duelo-pixel`, Space Invaders/`invasores`, Frogger/`ranaria`, Pac-Man/`gloton`) más dieciséis ids nuevos (2048/`fusion`, Tron/`estela`, Missile Command/`impacto`, Simón/`secuencia`, Boxeo/`nocaut`, Memorice/`parejas`, Galería/`galeria`, Conecta 4/`conecta4`, Whack-a-mole/`topos`, Buscaminas/`minas`, Flappy Bird/`vuelo`, Gato/`gato`, Ahorcado/`verdugo`, Mastermind/`codigo`, Nim/`montones`, Lights Out/`apagon`) — todas en estado `propuesto`, ninguna aceptada, descartada ni implementada todavía. **Esta fue la ronda final (20/20) de la tanda pedida por el usuario**; no se generarán más propuestas automáticamente a menos que el usuario reabra la serie o pida explícitamente evaluar una ronda 21. Quedan ocho candidatos en `considerado` como cola de respaldo permanente si hiciera falta revisar/reemplazar alguno de los veinte `propuesto` (en orden de esfuerzo estimado): `Lights Out` recién promovido a `propuesto`, luego `Solitario (Klondike)`, `Reversi`, `Buscapalabras`, `Match-3`, `Combate de tanques`/`Q*bert` (equivalentes en esfuerzo), y por último `Centipede` y `Damas (Checkers)` (los dos más caros del historial). Recomendación explícita para el usuario antes de pedir una ronda 21: priorizar revisar, aceptar o implementar los veinte candidatos ya propuestos —empezando por los de menor esfuerzo (`verdugo`, `codigo`, `montones`, `apagon`, `vuelo`) y por los cuatro placeholders sin migración— antes de seguir generando ideas nuevas desde cero.
